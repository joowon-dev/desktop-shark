# 바탕화면 상어

바탕화면 위에 얹히는 투명 오버레이에서 **상어 한 마리를 키운다.** 상어는 평소 숨어
있고, 가끔 검은 실루엣과 등지느러미로 스쳐 지나간다. 밥을 주면 달려와 먹고, 많이
먹을수록 커지고 또렷해진다. 오래 굶기면 화면을 맴돌며 보챈다.

「몰래 시리즈」가 아니다. **숨기는 것이 목적이 아니라 같이 사는 것이 목적**이라,
불꽃놀이와 같은 셋째 갈래에 놓는다.

```bash
npm start          # 빌드 → 돌던 앱 종료 → 새로 실행 (맥)
npm test
npm run test:watch
npm run icons      # build/icon.png · build/tray.png · windows/icon.ico 를 다시 그린다

npm run build:mac  # dist/DesktopShark-mac.zip (임시 서명 — 남에게 주면 막힌다)
npm run build:win  # dist/DesktopShark-win-x64.zip (맥에서도 빌드된다)
```

조작:

| 키 | 하는 일 |
|---|---|
| `⌥⇧S` (윈도우 `Alt+Shift+S`) | **게임모드 켜기/끄기** — 켜면 클릭이 밥, 끄면 완전 통과 |
| `⌥⇧R` (윈도우 `Alt+Shift+R`) | 랭킹·계정 패널 |
| `⌥⇧H` (윈도우 `Alt+Shift+H`) | 숨기기 / 보이기 |
| 메뉴바 🦈 (윈도우 트레이) | 단계·배고픔 · 게임모드 · 랭킹 · 모니터 · 놓아주기 · 종료 |

---

## 이 앱이 시리즈의 다른 앱과 다른 점

시리즈의 공통 규칙은 `../CLAUDE.md` 에 있다. 여기 적는 것은 **상어만 다른 것**이다.

### 1. 클릭을 받는다 — 게임모드

4구·불꽃놀이는 마우스를 영영 안 받지만 상어는 **밥을 클릭으로 준다.** 4구가
「수식키를 누르고 있는 동안」 마우스를 받던 자리(`updateMousePass(holding:)`)를
「게임모드가 켜진 동안」으로 바꾼 것이 전부다. 수식키 폴링 타이머는 통째로 없다.

**게임모드가 켜진 것을 눈으로 알 수 있어야 한다.** 켜진 동안은 밑의 앱을 못 누르는데
그걸 모르면 「컴퓨터가 고장났다」가 된다. 그래서 세 군데에서 알린다 — 메뉴바 아이콘이
🦈 → 🐟, 화면 가장자리의 물빛 테두리, 화면 위쪽의 눈금. 그리고 **창을 숨기면 게임모드도
강제로 꺼진다**(`toggleWindow`) — 안 보이는 창이 클릭을 먹는 상태를 만들지 않는다.

### 2. 「첫 클릭」을 삼키지 않아야 한다 — `acceptsFirstMouse`

**이 앱에서 제일 오래 붙잡은 버그다.** 맥에서 게임모드를 켜고 `ignoresMouseEvents` 를
꺼도 밥이 한 번도 안 떨어졌다. AppKit 은 활성화되지 않은 앱의 창을 클릭하면 그 클릭을
「앱을 앞으로 부르는 클릭」으로 쓰고 뷰에는 넘기지 않는다. 이 앱은 포커스를 절대 안
가져가므로 **모든 클릭이 첫 클릭**이다.

`FirstMouseWebView` 가 `acceptsFirstMouse` 에 `true` 를 돌려주는 한 줄로 끝난다.
4구·불꽃놀이는 클릭을 안 써서 이 벽을 만난 적이 없다 — **시리즈에서 처음 밟은 자리다.**

비슷한 이유로 CSS 쪽에도 함정이 하나 있었다. `#stage` 에 `pointer-events: none` 을
걸어 두면 창이 클릭을 받아도 캔버스가 안 받는다. **창째로 흘려보내는 일은 셸이 한다 —
페이지 안에서 또 막으면 안 된다.** 테스트 92 개가 전부 초록인 채로 살아남은 버그다.

### 3. 별명을 치려면 창이 키보드를 받아야 한다

패널에서 별명을 타이핑해야 하는데, 테두리 없는 창은 기본적으로 키 윈도우가 못 된다
(윈도우에서는 `WS_EX_NOACTIVATE` 가 같은 일을 한다). **패널을 열 때만** 포커스를
가져가고 닫으면 곧장 돌려준다(`setPanel`). 평소에는 절대 안 가져간다.

### 4. 투명은 픽셀 알파로 만든다 (윈도우)

상어는 화면의 거의 모든 픽셀이 반투명이라 컬러 키를 쓰면 실루엣마다 자홍색 테두리가
낀다. 불꽃놀이와 같은 이유로 `DwmEnableBlurBehindWindow` 를 쓴다.

---

## 모듈 경계

`src/game/` 은 전부 순수 ES 모듈이다. `window`·`document`·`Date.now()`·`Math.random()`
을 쓰지 않는다.

| 파일 | 하는 일 | 모르는 것 |
|---|---|---|
| `constants.js` | 튜닝 값 전부 | — |
| `rng.js` | 씨앗 난수 | — |
| `swim.js` | 조향. 목표점 쪽으로 한 스텝. 선회 반경을 못 넘는다 | 상태도 밥도 모른다 |
| `brain.js` | 상태기계. 전이와 목표점 | 어떻게 헤엄치는지 모른다 |
| `hunger.js` | 흐른 시간 → 배고픔 `0..1` | 상어를 모른다 |
| `growth.js` | 누적 먹이 → 단계 → 크기·선명도 | 시간을 모른다 |
| `food.js` | 밥의 가라앉음·수명·먹힘 판정 | 상어의 상태를 모른다 |
| `sync.js` | 서버에 무엇을 보낼지. 밀린 줄 | 네트워크를 모른다 |
| `engine.js` | 고정 타임스텝 `1/60` 으로 위를 묶는다 | 그리는 법을 모른다 |

**`brain` 과 `swim` 이 서로를 모르는 것이 이 앱 설계의 핵심이다.** `brain` 은 「저기로
가고 싶다」만 말하고 `swim` 은 「이 방향에서 저기까지는 이렇게 돈다」만 안다. 그래서
상어가 밥을 지나쳤다가 크게 선회해 돌아오는 움직임이 **따로 짜지 않아도** 나온다 —
`brain` 은 계속 밥을 가리키고 `swim` 이 못 꺾을 뿐이다. 둘을 합치면 이 움직임이 사라진다.

`TURN_RATE`(초당 최대 선회 각도)가 이 앱에서 제일 중요한 한 값이다. 이걸 키우면 상어가
제자리에서 홱 도는 순간 물고기가 아니라 커서가 된다.

## 상태기계

```
숨음 --(20~45초 랜덤)--> 순찰 --(화면 밖)--> 숨음
숨음·순찰 --(배고픔 ≥ 0.7)--> 에워쌈 --(배고픔 < 0.7)--> 숨음
무엇이든 --(밥 있음)--> 돌진 --(닿음)--> 먹음 --(0.25초)--> 만족
만족 --(밥 남음)--> 돌진 │ --(6초)--> 숨음
```

**밥이 떨어지면 어떤 상태에서든 즉시 `돌진`** 이다. 이것이 게임의 약속이라 예외를 두지
않는다(먹는 중 0.25 초만 빼고 — 한 입은 끝까지 문다).

## 가시성 — 곱셈 한 자리

```
알파 = 단계기본선명도 × 상태배율 × (게임모드면 1.6)
```

「많이 먹일수록 또렷해진다」가 여기서 나온다. 단계를 올리면 **숨어 있을 때(배율 0)만
빼고** 전부 진해진다. 상한 `MAX_ALPHA = 0.85` 는 바탕화면을 가리지 않으려는 것이다.

## 저장하는 값은 둘뿐

**누적 먹이 수와 마지막으로 먹인 시각.** 단계도 배고픔도 이 둘에서 계산하므로 따로
저장하지 않는다 — 저장한 값과 계산한 값이 어긋날 자리를 아예 없앤다.

배고픔은 **마지막으로 먹인 벽시계 시각**에서 잰다. 프레임에서 누적하면 껐다 켠 시간이
사라져 「하루 만에 켜니 배고파 있다」가 안 된다.

---

## 랭킹 — 몰래 야구와 **같은 DB**

Supabase 프로젝트 `xajmblrdkdnqoxfvsfrt` (SNEAKY_BASEBALL) 를 같이 쓴다.
`public.players` 가 공용 계정이라 **별명과 복구 코드가 두 게임에서 하나**다. 야구에서
쓰던 복구 코드를 패널에 넣으면 상어가 같은 이름으로 붙는다. 그래서 `sync.js` 의
`parseRecoveryCode` 는 야구의 것과 **규칙이 같아야 한다** — 갈라지면 야구 코드를 넣었을
때 「모양이 틀렸다」가 된다.

기존 표(`players`·`cheers`)와 기존 함수는 건드리지 않았다. 더한 것만 적는다.

| 이름 | 무엇 |
|---|---|
| `public.sharks` | `player_id`(PK) · `eaten` · `last_fed_at` · `minute_start`/`minute_count` |
| `shark_stage(eaten)` | 단계 문턱. **`src/game/growth.js` 의 `STAGE_THRESHOLDS` 와 같은 값이어야 한다** |
| `feed_shark(player, secret, count)` | 먹인 횟수를 올린다. 분당 120 이 상한, 한 번에 20까지 |
| `shark_ranking(limit)` · `my_shark(player)` | 읽기. **기간 탭이 없다** |

RLS 를 켜고 **정책을 하나도 만들지 않았다** — 익명 키로는 표에 못 닿고, 위
`SECURITY DEFINER` 함수들만이 통로다. `cheers`·`players` 와 같은 방식이다.

**단계를 클라이언트가 보내지 않는다.** 먹인 횟수만 보내고 단계는 서버가 매긴다. 받아
적으면 앱을 뜯은 사람이 6단계를 적어 넣는다 — 야구의 `submit_hit` 이 점수를 안 받는
것과 같은 이유다.

**기간 탭이 없는 것도 결정이다.** 성장은 쌓이기만 하고 되돌아오지 않아서 「오늘의
성장」은 이 게임에서 뜻이 없다.

**계정이 없어도 게임은 전부 된다.** 랭킹에 안 올라갈 뿐이고, 네트워크 실패는 조용히
삼킨다. 화면에 쓰는 누적치는 `max(로컬, 서버)` 다 — 오프라인에서 키운 것을 잃지 않는다.

---

## 테스트

92 개. **전부 일부러 깨서 실패하는 것을 확인했다.** 아래 일곱 가지를 변조했을 때
각각 1~5 개가 빨개졌다.

| 무엇을 깼나 | 빨개진 테스트 |
|---|---|
| 선회 반경 상한 제거 | 2 |
| 단계 문턱 5 → 4 | 3 |
| 숨음 상태에서 밥 무시 | 4 |
| 배고픔 6시간 → 3시간 | 3 |
| 밥이 가득 차면 오래된 것 밀어내기 | 1 |
| 게임모드 선명도 배율 제거 | 2 |
| 먹힘 판정 반경 0 | 5 |

상수를 바꾸는 변경이면 **옛 값으로 되돌려 새 테스트가 실패하는지 확인하고** 커밋한다.

그리고 **테스트가 다 통과해도 눈으로만 보이는 것이 있다.** 이 앱에서만 두 개를 그렇게
찾았다 — 캔버스의 `pointer-events: none`(밥이 한 번도 안 떨어짐), 지느러미를 반투명으로
따로 칠해 생긴 이음매와 nonzero 감김이 뚫은 구멍. 물리나 조작이나 그림을 만지면
**앱을 띄워서 직접 해 본다.**

브라우저로 볼 때는 ES 모듈이라 `python3 -m http.server` 로 띄워서 연다. 셸이 없으면
저장도 핫키도 없이 게임만 돈다(`window.sneaky` 가 없으면 조용히 그 길로 간다).

## 지금 열려 있는 것

- **윈도우는 컴파일만 됐고 실행은 한 번도 안 해 봤다.** 확인할 것: ① 오버레이가
  투명하게 뜨는지(안 되면 DWM 픽셀 알파 경로 문제) ② `Alt+Shift+S` 로 게임모드를 켜고
  **빈 바탕화면을 클릭하면 밥이 떨어지는지** ③ `Alt+Shift+R` 패널에서 별명이 **타이핑
  되는지**(`WS_EX_NOACTIVATE` 를 떼는 부분).
- **리모트가 없다.** `master` 브랜치 하나뿐이다.
- **맥 배포본은 공증이 필요하다** — `./mac/notarize.sh`(키체인 프로파일 `webswing-notary`).
  `build.sh` 산출물은 임시 서명이라 Gatekeeper 가 막는다. **이 앱에서는 아직 한 번도 안 돌렸다.**
- 소리, 여러 마리, 수족관은 첫 판에 넣지 않았다.
