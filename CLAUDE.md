# 바탕화면 상어

바탕화면 위에 얹히는 투명 오버레이에서 **상어 한 마리를 키운다.** 상어는 평소 숨어
있고, 가끔 검은 실루엣과 등지느러미로 스쳐 지나간다. 많이 먹을수록 커지고 또렷해지고,
오래 굶기면 화면을 맴돌며 보챈다.

**밥은 일하다 저절로 준다.** 클릭하면 그 자리에 큰 밥, 타자를 치면 아무 데나 작은 밥이
떨어진다. 창은 클릭도 키도 **삼키지 않는다** — 누르던 버튼은 그대로 눌리고 치던 글자는
그대로 찍힌다. 그래서 켜 둔 채로 하루를 보내는 앱이고, **끄는 것은 남이 화면을 볼 때뿐**이다.

「몰래 시리즈」가 아니다. **숨기는 것이 목적이 아니라 같이 사는 것이 목적**이라,
불꽃놀이와 같은 셋째 갈래에 놓는다.

```bash
npm start          # 빌드 → 돌던 앱 종료 → 새로 실행 (맥)
npm test
npm run test:watch
npm run icons      # build/icon.png · build/tray.png · windows/icon.ico 를 다시 그린다

npm run build:mac  # dist/DesktopShark-mac.zip (임시 서명 — 남에게 주면 막힌다)
npm run build:win  # dist/DesktopShark-win-x64.zip (맥에서도 빌드된다)
```

조작:

| 키 | 하는 일 |
|---|---|
| `⌥⇧S` (윈도우 `Alt+Shift+S`) | **밥 주기 멈추기/다시.** 기본은 켜짐이다 |
| `⌥⇧R` (윈도우 `Alt+Shift+R`) | 랭킹·계정 패널 (패널 오른쪽 위 × 로도 닫는다) |
| `⌥⇧H` (윈도우 `Alt+Shift+H`) | 숨기기 / 보이기 |
| 메뉴바 🦈 (윈도우 트레이) | 단계·배고픔 · 밥 주기 · 랭킹 · 모니터 · 놓아주기 · 종료 |

**밥 주기는 저장하지 않는다.** 앱을 띄우면 늘 켜진 채로 시작한다 — 끈 것은 「지금 잠깐」
이라는 뜻이지 설정이 아니다.

---

## 이 앱이 시리즈의 다른 앱과 다른 점

시리즈의 공통 규칙은 `../CLAUDE.md` 에 있다. 여기 적는 것은 **상어만 다른 것**이다.

### 0. 상어 종류와 도감

여섯 종이 있고 **모양만 다르고 규칙은 같다** — 어떤 종이든 밥을 먹고 여섯 단계로 자란다.

실루엣은 `species.js` 의 숫자 몇 개(`depth`·`snout`·`dorsal`·`tail`·`size`·`speed`)로
정한다. **종마다 윤곽을 통째로 그려 두지 않는다** — 여섯 벌을 따로 손봐야 하고, 한 종만
고쳐도 나머지가 어긋난다. 망치머리와 톱코만 `extras` 로 조각을 더 얹는다.

그리는 함수는 하나다(`drawSharkBody`). **도감도 같은 함수를 쓴다** — 도감의 그림과
화면의 상어가 다르면 도감이 아니다.

도감에 오르는 규칙: **6단계에 닿는 순간 저절로 오른다**(`engine.step`). 따로 누를 것이
없다 — 다 키운 것을 알아채지 못하면 도감이 안 찬다. 다 큰 뒤에 패널에서 놓아주면
(`releaseAndNext`) **아직 도감에 없는 종**의 아기가 온다. 이미 모은 종이 또 오면 모으는
일이 운에 맡겨진다.

그릴 때 **중점 곡선 보간이 뾰족한 점 하나를 반쯤 뭉갠다.** 환도상어의 긴 꼬리를 점
하나로 두었더니 백상아리와 똑같아졌다 — 위 갈래를 점 둘로 그려야 길이가 산다.
망치머리는 `addSharp` 로 그린다. 곡선으로 이으면 혹이 달린 상어가 된다.

### 1. 창은 마우스를 안 받는다. 대신 엿듣는다

**여기가 이 앱에서 제일 중요한 결정이다.**

처음에는 4구처럼 「밥 주는 동안 창이 클릭을 받는다」로 만들었다. `ignoresMouseEvents`
를 껐다 켜는 방식이다. 그런데 밥 주기는 **늘 켜져 있어야 하는 기능**이라, 그대로 두면
켜 둔 내내 밑의 앱을 아무것도 못 누른다 — 켜고 쓸 수 있는 앱이 아니게 된다.

그래서 뒤집었다. **창은 마우스를 영영 안 받고**(패널을 열 때만 예외), 셸이 클릭과
타자를 **전역으로 엿듣기만** 해서 게임에 알려 준다.

| | 맥 | 윈도우 |
|---|---|---|
| 엿듣기 | `CGEventSource.counterForEventType` 을 60Hz 로 **물어본다** | `SetWindowsHookEx(WH_MOUSE_LL / WH_KEYBOARD_LL)` |
| 위치 | `NSEvent.mouseLocation` | 훅이 준다 |
| 권한 | **필요 없음** | 필요 없음 |

**맥에서 권한을 안 받는 것이 이 앱에서 두 번째로 중요한 결정이다.**

처음에는 `addGlobalMonitorForEvents` 로 keyDown 을 받아 봤다. 삼키지는 않지만 **읽을
수는 있어서** 손쉬운 사용 권한을 받아야 했다. 셀 수만 있으면 되는 일에 읽을 수 있는
길을 여는 것은 과했고, 권한을 안 주면 **클릭은 되는데 타자만 조용히 안 되는** 상태가
됐다(에러도 없고 콜백만 영영 안 불린다).

지금은 `CGEventSource` 의 **통계 카운터**를 본다. 「keyDown 이 지금까지 몇 번 있었나」를
물어보는 것이라 권한이 필요 없다 — **권한이 없는 번들로 실제로 확인했고**, 앱에서도
`tccutil reset` 으로 권한을 지운 뒤 타자가 밥이 되는 것을 확인했다. 어떤 키였는지는
**알 수가 없다.** 「몇 번 쳤는지만 센다」가 설명이 아니라 사실이 됐다.

카운터는 밥을 안 주는 동안에도 흐르므로 **매 번 따라만 둬야 한다.** 안 그러면 다시 켤
때 그동안 친 것이 한꺼번에 쏟아진다.

윈도우 훅은 **델리게이트를 필드로 들고 있어야 한다.** 지역 변수로 두면 GC 가 거둬 가고
그때부터 훅이 조용히 죽는다.

### 2. 한때 밟았던 함정 둘 — 이제는 안 쓰지만 남겨 둔다

클릭을 창으로 받던 시절에 이틀치 시간을 먹은 두 가지다. 다시 그 길로 돌아가려 한다면
먼저 읽을 것.

- **`acceptsFirstMouse`** — AppKit 은 활성화되지 않은 앱의 창을 클릭하면 그 클릭을
  「앱을 앞으로 부르는 클릭」으로 쓰고 뷰에는 안 넘긴다. 이 앱은 포커스를 절대 안
  가져가므로 **모든 클릭이 첫 클릭**이다. `FirstMouseWebView` 가 아직 남아 있고,
  패널을 누를 때 그대로 필요하다.
- **CSS 의 `pointer-events: none`** — 창이 클릭을 받아도 캔버스가 안 받는다. 창째로
  흘려보내는 일은 셸이 하는 것이고 페이지 안에서 또 막으면 안 된다. 지금은 클릭이
  애초에 창으로 안 오므로 캔버스도 눈금도 다시 `none` 이다.

둘 다 **에러 없이 조용히 「아무 일도 안 일어남」**이 되고, 테스트 92 개가 전부 초록인
채로 살아남았다.

### 3. 패널은 마우스만 받는다. 키보드는 별명 칸을 누를 때만

패널은 눌러야 하고 별명은 쳐야 한다. 처음에는 패널을 열 때 둘 다 가져왔는데, 그러면
**패널을 띄워 둔 채로 다른 창에 한 글자도 못 친다** — 랭킹을 보면서 일할 수가 없다.

지금은 **마우스만** 받고(`updateMousePass` 가 보는 것은 패널 하나뿐이다), 키보드는
별명 칸에 `focus` 가 갔을 때만 가져온다(`grabKeyboard`). 칸에서 손을 떼거나 패널을
닫으면 곧장 돌려준다. 맥은 `NSApp.activate`/`deactivate`, 윈도우는 `WS_EX_NOACTIVATE`
를 뗐다 붙였다 한다.

패널은 **끌어서 옮긴다.** 창틀이 없으니 제목줄(`#grip`)이 손잡이다. **자리는 기억하지
않는다** — 화면 크기가 바뀌면 기억한 자리가 화면 밖일 수 있고, 그러면 패널을 영영 못
찾는다.

### 4. 투명은 픽셀 알파로 만든다 (윈도우)

상어는 화면의 거의 모든 픽셀이 반투명이라 컬러 키를 쓰면 실루엣마다 자홍색 테두리가
낀다. 불꽃놀이와 같은 이유로 `DwmEnableBlurBehindWindow` 를 쓴다.

---

## 모듈 경계

`src/game/` 은 전부 순수 ES 모듈이다. `window`·`document`·`Date.now()`·`Math.random()`
을 쓰지 않는다.

| 파일 | 하는 일 | 모르는 것 |
|---|---|---|
| `constants.js` | 튜닝 값 전부 | — |
| `rng.js` | 씨앗 난수 | — |
| `swim.js` | 조향. 목표점 쪽으로 한 스텝. 선회 반경을 못 넘는다 | 상태도 밥도 모른다 |
| `brain.js` | 상태기계. 전이와 목표점 | 어떻게 헤엄치는지 모른다 |
| `hunger.js` | 흐른 시간 → 배고픔 `0..1` | 상어를 모른다 |
| `growth.js` | 누적 먹이 → 단계 → 크기·선명도 | 시간을 모른다 |
| `food.js` | 밥 두 종류의 가라앉음·수명·먹힘 판정 | 상어의 상태를 모른다 |
| `species.js` | 여섯 종의 몸 비율과 도감 | 그리는 법도 시간도 모른다 |
| `sync.js` | 서버에 무엇을 보낼지. 밀린 줄 | 네트워크를 모른다 |
| `engine.js` | 고정 타임스텝 `1/60` 으로 위를 묶는다 | 그리는 법을 모른다 |

**`brain` 과 `swim` 이 서로를 모르는 것이 이 앱 설계의 핵심이다.** `brain` 은 「저기로
가고 싶다」만 말하고 `swim` 은 「이 방향에서 저기까지는 이렇게 돈다」만 안다. 그래서
상어가 밥을 지나쳤다가 크게 선회해 돌아오는 움직임이 **따로 짜지 않아도** 나온다 —
`brain` 은 계속 밥을 가리키고 `swim` 이 못 꺾을 뿐이다. 둘을 합치면 이 움직임이 사라진다.

`TURN_RATE`(초당 최대 선회 각도)가 이 앱에서 제일 중요한 한 값이다. 이걸 키우면 상어가
제자리에서 홱 도는 순간 물고기가 아니라 커서가 된다.

## 밥 두 종류

| | 무엇으로 | 값 | 크기 |
|---|---|---|---|
| 큰 밥 | 클릭한 자리 | **10** | 아주 작고 심지가 있다 |
| 작은 밥 | 타자 — 자리는 무작위 | **1** | 더 작다 |

**값이 열 배 달라야 한다.** 클릭은 드물고 일부러 하는 것, 타자는 잦고 무심코 하는
것이다. 비슷하면 타이핑만으로 며칠 만에 다 커 버려서 키우는 일이 사라진다 — 타자는
「조금씩 쌓이는 배경」이고 클릭이 「먹이는 행위」다.

**둘 다 거의 안 보인다.** 바탕화면 위에 늘 떠 있는 것이라 보이는 순간 거슬린다.
**상어가 그쪽으로 헤엄쳐 오는 것으로 「저기에 밥이 있다」를 안다** — 밥 자체를 볼
필요가 없다. 물자국도 같은 이유로 아주 연하다(알파 0.13). 물자국이 상어보다 눈에 띄면
화면에 흰 줄이 그어진 것으로 보인다.

**상어도 작다.** 다 큰 백상아리가 화면 높이의 1/12 쯤이다(고래상어도 1/7 을 안 넘는다).
그래서 **입 크기를 몸 길이에 비례**시켜야 했다(`EAT_REACH`) — 고정값으로 두면 아기상어가
제 몸의 두 배 떨어진 밥을 삼켜서, 헤엄쳐 가서 무는 장면이 사라진다.

그리고 **타자는 `TYPE_FEED_INTERVAL`(0.6초)에 한 번만 받는다.** 사람은 1 분에 400 타도
치므로 그대로 받으면 밥이 순식간에 가득 찬다. 넘치는 입력은 **줄을 세우지 않고 버린다** —
줄을 세우면 손을 뗀 뒤에도 한참 떨어져서 「내가 친 것」이라는 느낌이 끊긴다.

상어가 노릴 밥은 `bestFood` 가 고른다. **가까운 것이 아니라 「가까우면서 값진 것」**이다.
거리만 보면 타자로 흩뿌려진 작은 밥에 밀려 큰 밥을 영영 안 먹는다.

## 화면 밖으로 안 나간다

**이게 이 게임의 전제다.** 나가 버리면 「바탕화면에 산다」가 아니라 「가끔 지나간다」가
되고, 밥을 줘도 한참을 기다려야 한다. 처음에는 `순찰` 이 화면을 벗어나는 것으로 끝나고
`숨음` 은 화면 밖 먼 점을 목표로 삼았는데, 띄워 보니 상어가 거의 안 보였다.

지금은 **모든 목표가 `keepInside` 를 거친다.** 가장자리 띠(`WALL_MARGIN`) 안에 들어오면
목표를 안쪽으로 밀어서 스스로 크게 선회해 돌아 나온다 — **위치를 억지로 붙잡지 않는다.**
붙잡으면 벽에 부딪혀 미끄러지는 모양이 나오고, 그건 물고기가 아니다.

예외가 하나 있다: **밥 쪽으로는 벽 보정을 안 건다.** 걸면 가장자리에 떨어뜨린 밥을
영영 못 먹는다.

`순찰` 도 이제 **시간으로 끝난다**(`CRUISE_MIN`~`CRUISE_MAX`). 화면 밖으로 나가서
끝나지 않는다.

## 상태기계

```
숨음 --(20~45초 랜덤)--> 순찰 --(화면 밖)--> 숨음
숨음·순찰 --(배고픔 ≥ 0.7)--> 에워쌈 --(배고픔 < 0.7)--> 숨음
무엇이든 --(밥 있음)--> 돌진 --(닿음)--> 먹음 --(0.25초)--> 만족
만족 --(밥 남음)--> 돌진 │ --(6초)--> 숨음
```

**밥이 떨어지면 어떤 상태에서든 즉시 `돌진`** 이다. 이것이 게임의 약속이라 예외를 두지
않는다(먹는 중 0.25 초만 빼고 — 한 입은 끝까지 문다).

## 가시성 — 곱셈 한 자리

```
알파 = 단계기본선명도 × 상태배율 × (게임모드면 1.6)
```

「많이 먹일수록 또렷해진다」가 여기서 나온다. 단계를 올리면 **숨어 있을 때(배율 0)만
빼고** 전부 진해진다. 상한 `MAX_ALPHA = 0.85` 는 바탕화면을 가리지 않으려는 것이다.

## 저장하는 값은 둘뿐

**누적 먹이 수와 마지막으로 먹인 시각.** 단계도 배고픔도 이 둘에서 계산하므로 따로
저장하지 않는다 — 저장한 값과 계산한 값이 어긋날 자리를 아예 없앤다.

배고픔은 **마지막으로 먹인 벽시계 시각**에서 잰다. 프레임에서 누적하면 껐다 켠 시간이
사라져 「하루 만에 켜니 배고파 있다」가 안 된다.

---

## 랭킹 — 몰래 야구와 **같은 DB**

Supabase 프로젝트 `xajmblrdkdnqoxfvsfrt` (SNEAKY_BASEBALL) 를 같이 쓴다.
`public.players` 가 공용 계정이라 **별명과 복구 코드가 두 게임에서 하나**다. 야구에서
쓰던 복구 코드를 패널에 넣으면 상어가 같은 이름으로 붙는다. 그래서 `sync.js` 의
`parseRecoveryCode` 는 야구의 것과 **규칙이 같아야 한다** — 갈라지면 야구 코드를 넣었을
때 「모양이 틀렸다」가 된다.

기존 표(`players`·`cheers`)와 기존 함수는 건드리지 않았다. 더한 것만 적는다.

| 이름 | 무엇 |
|---|---|
| `public.sharks` | `player_id`(PK) · `eaten` · `last_fed_at` · `minute_start`/`minute_count` |
| `shark_stage(eaten)` | 단계 문턱. **`src/game/growth.js` 의 `STAGE_THRESHOLDS` 와 같은 값이어야 한다** |
| `feed_shark(player, secret, count)` | 먹인 **점수**를 올린다. 분당 400 이 상한, 한 번에 20까지 |
| `shark_ranking(limit)` · `my_shark(player)` | 읽기. **기간 탭이 없다** |

RLS 를 켜고 **정책을 하나도 만들지 않았다** — 익명 키로는 표에 못 닿고, 위
`SECURITY DEFINER` 함수들만이 통로다. `cheers`·`players` 와 같은 방식이다.

**단계를 클라이언트가 보내지 않는다.** 먹인 횟수만 보내고 단계는 서버가 매긴다. 받아
적으면 앱을 뜯은 사람이 6단계를 적어 넣는다 — 야구의 `submit_hit` 이 점수를 안 받는
것과 같은 이유다.

**기간 탭이 없는 것도 결정이다.** 성장은 쌓이기만 하고 되돌아오지 않아서 「오늘의
성장」은 이 게임에서 뜻이 없다.

**첫 밥을 준 순간 임의 별명으로 계정이 자동으로 만들어진다**(`ensureAccount`).
이름부터 정하라고 하면 대부분 패널을 열지 않고, 열지 않으면 랭킹이 비어 있다. 먼저
올려 두고 이름은 나중에 바꾸게 한다 — 바꿔도 같은 계정이다. 이름이 겹치면 몇 번 다시
뽑는다. 실패는 조용히 삼키고 다음 밥에서 다시 해 본다.

**랭킹은 껐다 켤 수 있다**(패널의 스위치, 메뉴바, `rankingOn` 에 저장). 끄면
**아무것도 서버로 안 보내고** 자동 등록도 안 한다. 상어는 그대로 자란다 — 랭킹은
게임이 아니라 곁다리이므로 꺼도 잃는 것이 없어야 한다. 밥 주기(`gameMode`)는 저장하지
않지만 이건 저장한다. 「안 올린다」는 그때그때가 아니라 설정이다.

**계정이 없어도 게임은 전부 된다.** 랭킹에 안 올라갈 뿐이고, 네트워크 실패는 조용히
삼킨다. 화면에 쓰는 누적치는 `max(로컬, 서버)` 다 — 오프라인에서 키운 것을 잃지 않는다.

---

## 테스트

141 개. **전부 일부러 깨서 실패하는 것을 확인했다.** 아래 스물넷을 변조했을 때 각각
하나 이상이 빨개졌다.

| 무엇을 깼나 | 빨개진 테스트 |
|---|---|
| 선회 반경 상한 제거 | 2 |
| 단계 문턱 5 → 4 | 3 |
| 숨음 상태에서 밥 무시 | 4 |
| 배고픔 6시간 → 3시간 | 3 |
| 밥이 가득 차면 오래된 것 밀어내기 | 1 |
| 밥 주기 선명도 배율 제거 | 2 |
| 먹힘 판정 반경 0 | 5 |
| 큰 밥 값 3 → 1 (작은 밥과 같게) | 3 |
| 타자 간격 0.6 → 0 | 2 |
| 밥 주기를 꺼도 클릭이 밥이 됨 | 1 |
| 밥 주기 기본값 켜짐 → 꺼짐 | 11 |
| 먹이 값 무시 (무조건 1) | 3 |
| 큰 밥 가중치 제거 (거리만) | 1 |
| 타자도 큰 밥을 떨어뜨림 | 1 |
| `keepInside` 무력화 (벽 보정 제거) | 4 |
| 상어를 옛 크기로 되돌림 | 1 |
| 입이 몸보다 커짐 (`EAT_REACH` 3.0) | 1 |
| 뱀상어를 백상아리와 똑같이 | 1 |
| 첫 상어를 톱상어로 | 1 |
| 이미 모은 종도 또 나오게 | 2 |
| 도감에 없는 종도 받아들이게 | 1 |
| 6단계에 닿아도 도감에 안 오르게 | 1 |
| 놓아줄 때 밀린 줄을 안 비움 | 1 |
| 종별 속력 무시 | 1 |

상수를 바꾸는 변경이면 **옛 값으로 되돌려 새 테스트가 실패하는지 확인하고** 커밋한다.

그리고 **테스트가 다 통과해도 눈으로만 보이는 것이 있다.** 이 앱에서만 두 개를 그렇게
찾았다 — 캔버스의 `pointer-events: none`(밥이 한 번도 안 떨어짐), 지느러미를 반투명으로
따로 칠해 생긴 이음매와 nonzero 감김이 뚫은 구멍. 물리나 조작이나 그림을 만지면
**앱을 띄워서 직접 해 본다.**

브라우저로 볼 때는 ES 모듈이라 `python3 -m http.server` 로 띄워서 연다. 셸이 없으면
저장도 핫키도 없이 게임만 돈다(`window.sneaky` 가 없으면 조용히 그 길로 간다).

## 지금 열려 있는 것

- **윈도우는 컴파일만 됐고 실행은 한 번도 안 해 봤다.** 확인할 것: ① 오버레이가
  투명하게 뜨는지(안 되면 DWM 픽셀 알파 경로 문제) ② **아무 앱이나 클릭하고 타자를 쳤을 때
  그 앱이 정상으로 동작하면서** 밥이 떨어지는지(저수준 훅이 이벤트를 삼키면 안 된다)
  ③ `Alt+Shift+R` 패널에서 별명이 **타이핑되는지**(`WS_EX_NOACTIVATE` 를 떼는 부분).
  백신이 저수준 키보드 훅을 막는 일이 있다 — 안 되면 `SHARK_DEBUG=1` 로 `엿듣기 시작`
  줄의 `key=` 를 볼 것.
- **맥은 어떤 권한도 필요 없다.** 예전에 필요했던 손쉬운 사용은 카운터 방식으로 바꾸며
  없앴다. 다시 `addGlobalMonitorForEvents` 로 돌아가지 말 것.
- **리모트가 없다.** `master` 브랜치 하나뿐이다.
- **맥 배포본은 공증이 필요하다** — `./mac/notarize.sh`(키체인 프로파일 `webswing-notary`).
  `build.sh` 산출물은 임시 서명이라 Gatekeeper 가 막는다. **이 앱에서는 아직 한 번도 안 돌렸다.**
- 소리, 여러 마리, 수족관은 첫 판에 넣지 않았다.
