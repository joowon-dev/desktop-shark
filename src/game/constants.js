// 튜닝 값 전부. 흩어 두면 손볼 때 못 찾는다.
//
// **「손맛」이라고 적힌 값은 계산에서 나온 것이 아니다.** 눈으로 보고 정한 값이라
// 바꿔도 아무 테스트가 안 깨질 수 있다. 4구의 DRAW_GAIN 이 그랬다.

/** 고정 타임스텝. 프레임 시간을 그대로 적분하면 기계마다 결과가 달라진다. */
export const DT = 1 / 60

// MARK: 헤엄

/** 순찰 속력 (화면 짧은 변 대비 비율/초). 손맛. */
export const CRUISE_SPEED = 0.10
/** 돌진은 순찰의 몇 배인가. 손맛 — 2.2 아래로는 「달려온다」는 느낌이 안 난다. */
export const DASH_MULTIPLIER = 2.2
/** 만족·에워쌈은 느긋하다. 손맛. */
export const SATED_SPEED = 0.06
export const PROWL_SPEED = 0.08

/**
 * 초당 최대 선회 각도(라디안). **이 앱에서 제일 중요한 한 값이다** —
 * 상어가 제자리에서 홱 도는 순간 물고기가 아니라 커서가 된다.
 * 1.6 rad/s ≈ 92°/s. 손맛.
 */
export const TURN_RATE = 1.6
/**
 * 돌진 중에는 훨씬 날카롭게 꺾는다. 덤벼드는 상어는 급하게 돈다.
 *
 * **선회 반경이 화면 밖으로 밀려나는 거리를 정한다.** 속력 0.22 에 이 값이 2.4 면
 * 반경이 0.09 라, 테두리 근처의 밥을 먹은 상어가 화면 밖으로 그만큼 나간다.
 * 4.2 면 0.05 다. 손맛이면서 동시에 「화면 안에 머문다」를 지키는 값이다.
 */
export const DASH_TURN_RATE = 4.2

/** 속력이 목표 속력에 붙는 빠르기(1/초). 손맛. */
export const SPEED_LERP = 3.0

// MARK: 밥

/** 화면에 동시에 떠 있는 밥의 최대 개수. 타자로도 떨어지므로 넉넉하다. */
export const MAX_FOOD = 10

/**
 * 밥 두 종류.
 *
 * **클릭은 드물고 일부러 하는 것, 타자는 잦고 무심코 하는 것**이라 값이 열 배 다르다.
 * 비슷하면 타이핑만으로 며칠 만에 다 커 버려서 키우는 일이 사라진다 — 타자는 「조금씩
 * 쌓이는 배경」이고 클릭이 「먹이는 행위」다.
 *
 * 크기는 작다. 바탕화면 위에 늘 떠 있는 것이라 눈에 띄면 일하는 데 거슬린다 —
 * 상어가 먹으러 오는 것이 보이면 충분하다. 둘 다 손맛이다.
 */
export const FOOD_KINDS = {
  big: { value: 10, radius: 0.0055 },
  small: { value: 1, radius: 0.0026 },
}

/**
 * 타자로 밥이 떨어지는 최소 간격(초). 연타로 화면을 채우지 않게 막는다.
 * 손맛 — 0.6 초면 빠르게 쳐도 1 분에 100 개 언저리다.
 */
export const TYPE_FEED_INTERVAL = 0.6
/** 밥이 사라지기까지(초). */
export const FOOD_LIFETIME = 20
/** 밥이 가라앉는 속도(화면 짧은 변 대비 비율/초). 손맛. */
export const FOOD_SINK_SPEED = 0.02

/**
 * 밥이 가라앉기를 멈추는 자리 — 화면 아래에서 이만큼 위.
 *
 * **밥이 화면 밖으로 내려가면 상어가 따라 내려간다.** 돌진할 때는 벽 보정을 안 걸기
 * 때문에(걸면 가장자리의 밥을 못 먹는다) 밥이 곧 상어가 갈 수 있는 가장 아래다.
 * 여기서 멈추고 바닥에 놓인 채로 수명을 다한다.
 */
export const FOOD_FLOOR = 0.14

/**
 * 밥이 화면 테두리에서 최소한 떨어져 있는 거리(화면 짧은 변 대비).
 *
 * 돌진하는 상어는 목표를 **지나친다** — 속력 0.22 에 선회 2.4 rad/s 라 선회 반경이
 * 0.09 쯤이다. 테두리에 딱 붙은 밥을 먹으면 그만큼 밖으로 밀려 나간다. 밥을 조금
 * 안쪽으로 당겨 두면 지나쳐도 화면 안이다. 클릭한 자리와 몇 픽셀 어긋나지만
 * 눈에 안 띈다.
 */
export const FOOD_INSET = 0.09
/**
 * 먹힘 판정의 **최소** 반경(화면 짧은 변 대비). 아기상어가 밥을 영영 못 무는 것을 막는다.
 */
export const EAT_RADIUS = 0.012

/**
 * 입의 크기는 몸 길이에 비례한다 — 몸 길이의 이만큼.
 *
 * 고정값으로 두면 상어를 줄였을 때 **입이 몸보다 커진다.** 아기상어가 제 몸 길이의
 * 두 배 떨어진 밥을 삼키면 헤엄쳐 가서 무는 장면이 사라진다.
 */
export const EAT_REACH = 0.45

// MARK: 배고픔

/** 배고픔이 1.0 에 닿기까지(초). 6 시간. 손맛 — 하루 한두 번 들여다보는 리듬. */
export const HUNGER_FULL_SECONDS = 6 * 60 * 60
/** 이 위로 올라가면 에워싼다. 손맛. */
export const PROWL_THRESHOLD = 0.7

// MARK: 상태 시간

/** 어슬렁거리다 순찰을 나가기까지(초). 이 사이에서 무작위. 손맛. */
export const LURK_MIN = 20
export const LURK_MAX = 45

/** 한 번 순찰하는 시간(초). 이 사이에서 무작위. 손맛. */
export const CRUISE_MIN = 10
export const CRUISE_MAX = 22

/**
 * 가장자리에서 되돌아 나오기 시작하는 거리(화면 짧은 변 대비).
 *
 * **상어는 화면 밖으로 나가지 않는다.** 나가 버리면 「바탕화면에 산다」가 아니라
 * 「가끔 지나간다」가 되고, 밥을 줘도 한참을 기다려야 한다. 이 띠 안에 들어오면
 * 목표를 화면 안쪽으로 끌어와서 스스로 크게 선회해 돌아 나온다.
 */
export const WALL_MARGIN = 0.18
/** 먹는 동안 멈칫하는 시간(초). */
export const EAT_DURATION = 0.25
/** 먹고 나서 느긋하게 도는 시간(초). */
export const SATED_DURATION = 6

// MARK: 성장

/**
 * 단계 문턱. 누적 먹이가 이 값 이상이면 그 단계다. **서버의 shark_stage 와 같아야 한다.**
 *
 * 큰 밥이 10 점이므로 클릭 100 번이면 다 큰다. 하루에 몇십 번 눌러 주면 한두 주,
 * 타자만 치면 훨씬 오래 걸린다 — 그 간격이 이 게임의 속도다.
 */
export const STAGE_THRESHOLDS = [0, 30, 100, 250, 550, 1000]

/**
 * 단계별 몸 길이 (화면 짧은 변 대비 비율). 손맛.
 *
 * **한 단계 오를 때마다 눈에 띄게 커진다** — 매번 1.35 배 이상이고 1 단계에서
 * 6 단계까지 6.9 배다. 키우는 맛이 여기서 나온다: 다음 단계가 「조금 큰 같은 상어」가
 * 아니라 「다른 상어」로 보여야 한다.
 *
 * **드라마는 아래쪽을 낮춰서 만든다.** 6 단계를 키우면 화면을 가려서 처음에 줄였던
 * 자리로 되돌아간다. 아기상어를 아주 작게 두면 같은 천장 안에서 차이가 벌어진다.
 * 다 큰 백상아리가 화면 높이의 1/9, 제일 큰 고래상어가 1/6 쯤이다.
 */
export const STAGE_LENGTHS = [0.016, 0.025, 0.038, 0.057, 0.080, 0.110]

/** 단계별 기본 선명도. 많이 먹일수록 또렷해진다. 손맛. */
export const STAGE_ALPHAS = [0.18, 0.24, 0.31, 0.39, 0.47, 0.55]

// MARK: 물자국
//
// **상어보다 눈에 띄면 안 된다.** 물자국이 진하면 바탕화면에 흰 줄이 그어진 것으로
// 보인다. 상어가 지나갔다는 것만 알면 되고, 그걸 알아채는 데는 아주 옅은 선으로 충분하다.

/** 자국이 남아 있는 시간(초). **draw 와 renderer 가 같은 값을 봐야 한다.** */
export const WAKE_LIFE = 0.85

/** 상어의 선명도에 이만큼 곱한 것이 자국의 선명도다. 손맛. */
export const WAKE_ALPHA = 0.05

/**
 * 선의 굵기와 벌어지는 폭 — **몸 길이 대비**다.
 *
 * 화면 대비로 두었더니 아기상어가 제 몸보다 굵은 자국을 남겼다. 상어가 단계마다
 * 크게 자라므로 자국도 같이 자라야 한다. 손맛.
 */
export const WAKE_WIDTH = 0.06
export const WAKE_SPREAD = 0.24

/** 먹은 자리에 퍼지는 동심원 — 몸 길이의 이만큼까지, 이 선명도로. 손맛. */
export const RIPPLE_REACH = 1.1
export const RIPPLE_ALPHA = 0.10

// MARK: 가시성

/**
 * 상태별 선명도 배율.
 *
 * **`lurk` 이 0 이 아니다.** 처음에는 0 이었고 이름도 `hidden` 이었다 —
 * 「숨어 있다 가끔 보인다」가 처음 설계였기 때문이다. 그런데 그러면 20~45 초 동안
 * 상어가 통째로 사라진다. 바탕화면에서 **같이 사는** 쪽으로 바뀐 뒤로는 그게 그냥
 * 없어지는 것으로 보인다. 지금은 제일 흐릴 때가 `lurk` 이고, 그래도 거기 있다.
 */
export const STATE_ALPHA = {
  lurk: 0.55,
  cruise: 1,
  prowl: 1.3,
  dash: 1.8,
  eat: 1.8,
  sated: 1.2,
}

/** 게임모드에서는 상어도 더 잘 보인다. 손맛. */
export const GAME_MODE_ALPHA = 1.6

/** 아무리 진해도 여기까지. 바탕화면을 가리면 안 된다. 손맛. */
export const MAX_ALPHA = 0.85

/**
 * 아무리 흐려도 여기까지. **상어는 사라지지 않는다.**
 *
 * 1 단계 상어가 제일 흐린 상태에 있으면 곱셈 결과가 0.08 까지 떨어져서 화면에서
 * 없어진 것처럼 보인다. 여기서 받친다 — 흐린 것과 없는 것은 다르다.
 *
 * **다만 알파만으로는 부족하다.** 상어는 거의 검정이라 어두운 바탕화면에서는
 * 알파를 올려도 검정 위의 검정이다. 그래서 그리는 쪽에서 밝은 테두리를 두른다
 * (draw.js 의 RIM). 밝은 배경은 몸이, 어두운 배경은 테두리가 보여 준다.
 */
export const MIN_ALPHA = 0.20
